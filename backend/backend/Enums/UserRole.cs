namespace backend.Enums
{
    public enum UserRole
    {
        DepartmentHead = 0,    // TP
        FacilityManager = 1,   // CSVC
        Department = 2,        // PB
        Lecturer = 3,          // GV
        Student = 4            // SV
    }
}
